sales owner line item

---
1. Contract는 어느 stage에서 만들어지나

Closed Won 입니다. 진입 조건은 두 개 AND:

StageName  =  Closed Won
ContractId =  비어 있음          ← 중복 생성 방지
+ "조건을 새로 충족하도록 업데이트된 경우"만

이 org의 Opportunity 단계는 RFQ 접수 → 숏리스트 선정 → 사양 협상 → 견적 제출 → 교체 제안 → 계약 검토 → Closed Won / Closed Lost 인데, 한글 단계에서는 아무것도 안 만들어집니다. 계약 검토 단계에서 생기는 게 아니라 Closed Won으로 넘어가는 순간입니다.

실행 순서는 4단계입니다:

Closed Won 진입
  ① Create_Contract   Contract 생성 (Status = Draft)
                      AccountId·Pricebook2Id는 Opportunity에서 상속
                      StartDate = 오늘,  ContractTerm = 12개월 (고정값)
  ② Activate_Contract Status → Activated 로 즉시 전환
  ③ Update_Opportunity Opportunity.ContractId ← 생성된 계약
  ④ Create_Order      Order 생성 (Status = Draft, EffectiveDate = 오늘)

Contract가 Draft로 태어나 곧바로 Activated가 되는 2단계 구조입니다. Contract를 바로 Activated로 만들 수 없어서 나눠놓은 형태입니다.

2. Contract ↔ Order 관계

Contract 1 : N Order — Order 쪽에 ContractId 룩업이 있습니다 (라벨 Contract Number).

flow는 Opportunity 1건당 Contract 1 + Order 1을 만들지만, 스키마상 한 계약에 주문이 여러 개 달릴 수 있고 실제로 그렇습니다. 예를 들어 계약 00000419 (삼양중공업 계열, 36개월) 하나에 주문 00000604 / 00000605 / 00000606 / 00000607 네 건이 매달려 있습니다. 장기 계약에 분할 납품이 걸린 형태죠.

연결 구조는 이렇습니다:

Opportunity ──ContractId──▶ Contract ◀──ContractId── Order
     │                          │                      │
     └──── AccountId ───────────┴──────────────────────┘

주의할 점: Order.OpportunityId가 이 org에는 존재하지 않습니다. flow의 Create_Order는 OpportunityId에 값을 넣고 있는데, 필드 조회에서는 ContractId와 AccountId만 잡힙니다. 즉 Order에서 Opportunity로 직접 거슬러 올라갈 수 없고, Contract를 경유해야 합니다. flow가 배포 시 통과한 걸 보면 필드는 있는데 조회에서 안 잡히는 것일 수 있으니, Order 리포트나 화면에서 영업기회를 보여줄 계획이라면 이 부분은 별도로 한 번 확인해보시는 게 좋겠습니다.

두 번째로, ContractTerm = 12개월이 하드코딩되어 있습니다. 계약 00000419처럼 36개월짜리는 수동으로 만들었거나 나중에 고친 것이고, flow가 만드는 계약은 전부 12개월로 나옵니다.

---
그러면여 흐름만 봐주세요. quote 고객이 승인해서 opty를 계약 검토 단계로 넘기고, 현실에서 계약사항 최종 협의하고 계약 검토를 마쳤다고 가정, closed won 되어서 contract는 바로 activate되며, order를 생성하게 됨(sales order겠죠) draft. 그리고, 추후 sap 내부에서 출하까지 완료되면, 출하 일자 기입하고, 그거 기반으로 work order 만들어서 설치 처리~. 이상 있을까요.

흐름 자체는 org 구성과 잘 맞습니다. 각 단계를 실제 flow에 대조해보고, 빠진 고리가 있는지 확인하겠습니다.

  Called salesforce, ran 1 shell command

흐름 자체는 맞습니다. org 구성과 대조하면 이렇게 이어집니다.

확인된 흐름

┌────────────────────────────────────────┬───────────────────────────────────────────┬─────────────┐
│                  단계                  │                   담당                    │  자동/수동  │
├────────────────────────────────────────┼───────────────────────────────────────────┼─────────────┤
│ Quote Accepted → Opportunity 계약 검토 │ Auto_Move_Stage_On_Quote_Accepted         │ 자동        │
├────────────────────────────────────────┼───────────────────────────────────────────┼─────────────┤
│ 계약 최종 협의                         │ —                                         │ 현실        │
├────────────────────────────────────────┼───────────────────────────────────────────┼─────────────┤
│ Opportunity Closed Won                 │ 사람                                      │ 수동        │
├────────────────────────────────────────┼───────────────────────────────────────────┼─────────────┤
│ Contract 생성 Draft → 즉시 Activated   │ Auto_Create_Contract_Order_On_Won         │ 자동        │
├────────────────────────────────────────┼───────────────────────────────────────────┼─────────────┤
│ Order 생성 Draft                       │ 같은 flow                                 │ 자동        │
├────────────────────────────────────────┼───────────────────────────────────────────┼─────────────┤
│ Draft → 제작 중 → 출하 완료            │ 없음                                      │ 수동        │
├────────────────────────────────────────┼───────────────────────────────────────────┼─────────────┤
│ 출하 완료 시 코디네이터 알림           │ Delivery_Alert_On_Order_Shipped           │ 자동        │
├────────────────────────────────────────┼───────────────────────────────────────────┼─────────────┤
│ 실제 출하일 기입                       │ 없음                                      │ 수동        │
├────────────────────────────────────────┼───────────────────────────────────────────┼─────────────┤
│ Work Order 생성 → 설치                 │ Order_To_WorkOrder (화면 flow)            │ 사람이 실행 │
├────────────────────────────────────────┼───────────────────────────────────────────┼─────────────┤
│ WO Completed → Order 납품 완료         │ Set_Order_Delivered_On_WorkOrder_Complete │ 자동        │
└────────────────────────────────────────┴───────────────────────────────────────────┴─────────────┘

마지막 줄이 말씀에 빠져 있었는데, Work Order가 완료되면 Related_Order__c를 타고 Order를 납품 완료로 자동 전환합니다. 루프가 닫힙니다. Order 상태도 Draft → 제작 중 → 출하 완료 → 납품 완료 4단계가 실제로 다 쓰이고 있습니다(각 78 / 104 / 9 / 314건).

이상한 점 — 하나 큽니다

① 자동 생성된 Order에 품목이 없습니다

OrderItem을 만드는 flow가 org에 하나도 없습니다. Auto_Create_Contract_Order_On_Won은 Order 헤더만 만들고 끝납니다. Quote Line Item이나 Opportunity Product를 Order로 옮기는 단계가 없어요.

방금 그 flow가 만든 주문들이 그 증거입니다:

00000611   Draft   TotalAmount 0
00000612   Draft   TotalAmount 0

반면 기존 주문들은 금액이 다 차 있습니다(납품 완료 314건 합계 약 2,708억). 즉 기존 데이터는 다른 경로로 품목이 들어왔고, 지금 flow로 새로 만들어지는 주문은 빈 껍데기입니다.

금액 0짜리 주문으로는 SAP 연계도, 매출 집계도, 출하 지시도 못 합니다. 실질적으로 여기가 끊겨 있습니다.

② 출하일 두 개 다 자동화 없음

계획 출하일은 참조하는 자동화조차 없고, 실제 출하일은 Order_To_WorkOrder가 읽기만 합니다. 그런데 이 값이 없으면 flow가 필수값 게이트에서 막아 Work Order를 못 만듭니다. 말씀하신 "SAP에서 출하 완료되면 기입"이 맞는데, 전부 수동이라는 점만 명확히 해두시면 됩니다.

③ Order 상태 전환도 전부 수동

Draft → 제작 중 → 출하 완료를 옮기는 자동화가 없습니다. SAP 연동이 들어올 자리로 보이는데, 지금은 사람이 드롭다운을 바꿔야 합니다.

④ 계약 기간이 전부 12개월 고정

ContractTerm = 12 하드코딩이라 노후화 교체든 신규 도입이든 똑같이 "오늘부터 1년"입니다.

정리

흐름 설계는 타당하고, 자동화 고리도 양끝(수주 → 계약·주문, WO 완료 → 납품 완료)은 잘 물려 있습니다. 가운데 구간(주문 품목 · 상태 전환 · 출하일)이 비어 있는데, ②③은 SAP가 채울 자리라면 의도된 공백입니다.

다만 ①은 SAP 이전 문제입니다. Salesforce가 만든 주문에 품목이 없으면 SAP로 넘길 내용 자체가 없으니까요. Quote Line Item → Order Product 복사를 flow에 넣을지 검토해보시면 좋겠습니다. 원하시면 어디에 어떻게 붙일지 짚어드리겠습니다.

order에 교체 대상 설비 asset 붙이는 flow가있는지 알려줫. 1번 품목은 quote에 있는 product 뭔가 붙일 방법이 있으려나아 (걍 sales order에서 붙여야지)


 order에 교체 대상 설비 asset 붙이는 flow가있는지 알려줫. 1번 품목은 quote에 있는 product 뭔가 붙일 방법이 있으려나아

  Called salesforce 4 times, ran 2 shell commands

1. 교체 대상 설비 — 필드는 있고, 채우는 flow는 없습니다

Order.Replaced_Asset__c (라벨 교체 대상 설비, Lookup(Asset)) 가 존재합니다. 그런데:

┌─────────────────────────┬─────────────────────────────┐
│                         │                             │
├─────────────────────────┼─────────────────────────────┤
│ 이 필드를 참조하는 flow │ Order_To_WorkOrder 단 하나  │
├─────────────────────────┼─────────────────────────────┤
│ 쓰는 flow               │ 없음 — 읽기 전용으로만 쓰임 │
├─────────────────────────┼─────────────────────────────┤
│ 실제 채워진 주문        │ 505건 중 6건 (1.2%)         │
└─────────────────────────┴─────────────────────────────┘

Order_To_WorkOrder는 이 값을 필수 게이트로 씁니다. 비어 있으면 「교체 대상 설비 필요」 화면을 띄우고 진행을 막습니다:

▎ 설비 교체 작업에는 교체 대상 설비가 반드시 필요합니다. 이 주문에 교체 대상 설비가 지정되어 있지 않습니다. 주문에서 먼저 지정한 후 다시 시도해 주세요.

값이 있으면 그걸 그대로 Work Order의 AssetId로 넘깁니다.

즉 노후화 교체 건은 수주 후 사람이 주문에서 설비를 직접 지정해야 Work Order를 만들 수 있습니다. 6/505라는 숫자를 보면 이 단계가 실제 운영에서 거의 안 지켜지고 있습니다.

자동화 여지는 있습니다 — Opportunity가 노후화 설비 교체 레코드 유형이면 대상 설비가 어딘가(Opportunity나 Case)에 이미 물려 있을 텐데, 그걸 Order로 옮겨주면 됩니다. 다만 Opportunity에 설비 룩업이 있는지부터 확인해야 해서, 필요하시면 그것부터 보겠습니다.

2. Quote 품목 → Order 품목, 가능합니다

매핑은 깔끔하게 떨어집니다. 실제 QuoteLineItem 값으로 보면:

Product2Id        01tgK00000DxVrdQAF   CP6000
PricebookEntryId  01ugK0000052KzBQAU   ← OrderItem 생성의 필수값, 그대로 재사용
Quantity          1
UnitPrice         1,251,000,000
Discount          20
TotalPrice        1,000,800,000

OrderItem이 요구하는 건 OrderId · PricebookEntryId · Quantity · UnitPrice 넷인데, 뒤 셋이 QuoteLineItem에 그대로 있습니다. Order에 Pricebook2Id도 이미 flow가 Opportunity에서 상속해 넣어주고 있어서 전제조건도 충족됩니다.

다만 걸림돌이 하나

Opportunity.SyncedQuoteId가 Closed Won 510건 전부 비어 있습니다.

Closed Won        510건
SyncedQuoteId 有    0건   ← 견적 동기화를 안 씀
ContractId 有     505건

견적 동기화(Sync) 기능을 안 쓰고 계셔서, "어느 견적이 최종인지"를 Opportunity에서 바로 알 수 없습니다. 그래서 flow에 이 단계를 붙일 때 최종 견적을 고르는 규칙을 먼저 정해야 합니다:

- Status = Accepted인 Quote 1건 — 지금 흐름상 가장 자연스럽습니다 (고객 수락 → 계약 검토 이동이 이미 그 신호를 쓰고 있으니까요)
- 또는 가장 최근 생성된 Quote
- 또는 Quote 동기화를 실제로 켜서 SyncedQuoteId를 쓰기

붙일 위치

Auto_Create_Contract_Order_On_Won의 마지막 Create_Order 뒤에 이어 붙이면 됩니다:

Create_Order (기존)
  └ Get_Accepted_Quote      Opportunity의 Accepted 견적 1건 조회
  └ Get_Quote_Line_Items    그 견적의 품목 전체 조회
  └ Loop                    품목마다
       └ Assign             OrderItem 레코드 조립 (OrderId + PBE + 수량 + 단가)
  └ Create_Order_Items      컬렉션 일괄 생성

이렇게 하면 자동 생성 주문의 TotalAmount가 0에서 실제 금액으로 채워지고, SAP로 넘길 내용도 생깁니다.

---

두 가지 다 실제로 만들어드릴까요? 만든다면 최종 견적 선정 규칙(위 3개 중 어느 것)만 정해주시면 됩니다. 그리고 교체 대상 설비 쪽은 Opportunity에 설비 룩업이 있는지 먼저 확인해봐야 하는데, 그것부터 볼까요?

✻ Baked for 1m 4s · done 11:15 PM

※ recap: We're mapping the Salesforce quote-to-order flow and found two gaps: auto-created Orders have no line items, and the 교체 대상 설비 field is never filled automatically. Next: pick the rule for choosing the final Quote, or check whether Opportunity has an Asset lookup.