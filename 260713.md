Dev
- first generation - on cloud
- second generation - force/apps

DB
- 오라클 trigger 필요 x
- Salesforce는 trigger 로직 구현할 필요 존재
	- custom explicit trigger <- trigger
	- standard implicit trigger  - hard to debug; debug log check;
Transaction
- 하나의 덩어리
- DB에 data commit되기까지 과정
- per - governor limits
	- Queries, DB operations, Heap space(memory), CPU time, Other
	- unrecoverable, unhandled - log 못 남김 (governor limit 초과시)
- Neither the field data type nor the api name may be changed
SOQL - STATEMENTS - DML
- IN / NOT IN - Inclusion / Exclusion
- DB 영국 시간대 default
- ![](Pasted%20image%2020260713115525.png)

sObject
- virual superclass
- object manager field & relationship
- SFDX: refresh sObject data
- Enable-sobject-refresh-on-startup
- Field Name Id PK(기본적으로 가져옴, record type도 가져옴) - 다른 object FK(Hyperlinks)
- fetch 가져올 때

?? null 이면 후행

Group by roll up / cube

비동기는 메모리 여유 있을 때만 돌아감

[] = Database.query('')

custom object \_\_r

relationship query - FK 기반

child 가져오면 nested sql 자식 이름 복수형

[] = Search.query('')
sosl - object 잘 모를 때