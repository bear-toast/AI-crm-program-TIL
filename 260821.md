- wo 조회 후 woli 업뎃 (ok)
- pricebook 반영 건 고려 (추후)
- wo, woli path 반영 (wo만) - vra 및 관련 필드 탭으로 빼기
- order to work order, case to work order (뭐로 만들지)
- 청구 대상 여부 product consumed 레이아웃에서 지워둘지 (read only)

```
ProductConsumed.Chargeable__c    읽기 전용
WOLI.Is_Billable__c              읽기 전용
WorkOrder.Is_Billable__c         읽기 전용
WorkOrder.Quoted_Amount__c       읽기 전용
```

**코디네이터 프로파일은 예외로 두셔도** 됩니다. 예외 상황에서 수동 조정이 필요할 수 있으니까요. 다만 조정해도 다음 Product Consumed 변경 시 덮어써진다는 건 알고 계셔야 합니다.

잠깐 product consumed 좀 걸리는데
Flow를 하나도 안 고쳐도 됩니다. 실무적으로도 **"부품을 꺼내 쓰기로 확정한 시점"**으로 해석하면 크게 어색하지 않고요. 재고 차감 관점에서는 오히려 맞습니다.

견적 승인    Case 커스텀 체크박스      진단 후 · 작업 전
작업 확인    Service Report 서명       작업 후

- 스킬 붙이는거 관련 (전기 설비 추가 필요) 완료 O
- 추천 부품  work type product required.. 관련 고민
- pricebook은 일단 하나 O
- order to work order 관련
- 자동 absence는 뭐람
---
## 전체 흐름 정리 (트리거 시점순)

### A. WO 생성 파이프라인 (설치/수리 등록 시 자동)

|#|Flow|트리거|하는 일|
|---|---|---|---|
|1|**WO 보증 판정 상속**|WO Create · Before|`WO.Is_Billable__c ← Case.Warranty_Determination__c` (시드값, 곧 롤업에 덮임)|
|2|**WOLI 생성**|WO Create · After|Work Type별 WOLI 세트 생성 → SA 일괄 생성 → Appointment Dependency(선행 체인) 생성 → 필요부품(ProductRequired) 복사|
|3|**WOLI 스킬 요구사항 생성**|WOLI Create · After|Subject 기반으로 SkillRequirement 생성(압축기/안전=공통, 시운전·배관 조건부)|
|4|**Work Order Rollup**|WOLI Create/Update · After|형제 WOLI의 `Is_Billable__c` 집계 → WO를 `유상/일부 유상/무상`으로 확정|

### B. Product Consumed 파이프라인 (엔지니어가 현장에서 소모품 등록/수정/삭제 시)

|#|Flow|트리거|하는 일|
|---|---|---|---|
|5|**PC Set Chargeable**|PC Create · Before|`Chargeable ← Product2.Is_Consumable`, 그리고 `Case.Warranty=유상`이면 강제 true (방금 수정본)|
|6|**PC Set WOLI Billable**|PC Create/Update · After|같은 WOLI에 유상 PC 있으면 `WOLI.Is_Billable=유상`, 없으면 무상 → (연쇄) #4 롤업|
|7|**PC Set WOLI Billable Delete**|PC Delete · Before|자기 제외 잔여 유상 PC로 WOLI 재판정 → (연쇄) #4 롤업|
|8|**PC Rollup Amount Save**|PC Create/Update · After|WO의 유상 PC `TotalPrice` 합산 → `WO.Quoted_Amount__c`|
|9|**PC Rollup Amount Delete**|PC Delete · Before|자기 제외 유상 PC 합산 → `WO.Quoted_Amount__c`|

**흐름 요약**: WO 만들면 A가 구조(WOLI/SA/스킬/의존성)를 깔고 유무상을 시드→롤업으로 확정. 이후 현장에서 PC를 넣고 빼면 B가 유무상(WOLI→WO)과 금액(WO)을 재계산. 방향은 **PC → WOLI → WO** 상향 단방향으로 일관됩니다.

시나리오상 치명적 결함은 없습니다. 9개 flow가 모두 활성이고, PC→WOLI→WO 상향 단방향으로 일관되게 맞물려 있습니다.

| 지점                           | 판정                                           |
| ---------------------------- | -------------------------------------------- |
| C/U/D 유무상·금액 재계산 연쇄          | 정상 — 삭제 포함 전 경로가 WOLI/WO로 전파                 |
| PC 삭제 시 잔여 합계 (self 제외)      | 정상 — 0건이면 0으로 갱신                             |
| #6/#8 중복 SOQL, #7/#9 중복      | 데모 규모 무해 (대량 시 거버너만 유의)                      |
| `Quoted_Amount__c` scale=0   | 원화면 무해 (소수 통화면 절사)                           |
| 진단 WOLI 무상 고정 → 롤업 `유상` 불가   | 의도된 동작, #5를 Case 참조로 바꿔 정합성 확보               |
| 로컬 `WO_WOLI_Generate`가 Draft | org는 v10 Active로 정상. 로컬 파일만 stale — 재배포 시 주의 |
Order ship to contact로 

현장 엔지니어 이메일 지현님으로

---
대화 전체를 압축해 정리했습니다. org `corepress` (aicrm_jmkimzzang77, trailsignup) 기준입니다.

---

# 대화 요약: Field Service 논의 & Order/Case → Work Order 스크린 플로우 설계

## 파트 1 — Resource Absence 자동 생성 여부 (결론: 미결, 보류)
- 질문: Dispatch Console에서 Schedule 배정 시 30분 absence가 자동으로 붙나?
- 조사 결과:
  - 로컬/서버 모두 absence를 **생성하는 커스텀 자동화 없음**. ResourceAbsence·ServiceAppointment 트리거는 전부 `FSL` 관리형 패키지 소속.
  - ResourceAbsence 7건 전부 `Vacation` 타입, 정확히 30분.
  - **리소스+종료시각이 5/5로 약속과 일치** → "일정 근무시간 이상이면 붙는다" 가설과 부합. 2시간짜리 SA-0061만 absence 없음(threshold 가설 지지).
  - 반대 증거: absence 5건이 한 타임스탬프(2026-08-20T16:13:59)에 일괄 생성 → 사용자가 "SA 일괄 선택 후 배치"라 설명. 시드/배치 흔적일 수도.
- **결론: 애매, 급하지 않아 보류.** 나중에 배정 1건 실행 → 직후 신규 `CreatedDate` absence 생기는지 보면 판정 가능.

## 파트 2 — Order/Case → Work Order 통합 스크린 플로우 (설계 확정, 생성 단계에서 막힘)

### 확정된 org 사실
- **WorkOrder RecordType 2종**: `Installation_Commissioning`(설치·시운전) / `Field_Service`(유무상 서비스)
- **WorkType 3종**(이름으로 다운스트림 분기): `신규 설치`(32h) / `설비 교체`(26h) / `고장 진단·조치`(6h)
- **발주 연결 필드는 `Related_Order__c`** (골자의 `발주__c` 아님)
- **다운스트림 자동화(활성) — 우리는 WorkOrder 1건만 생성. WOLI/SA 만들면 중복**:
  - `WorkOrder_Inherit_Warranty`(before-create): RecordType=Field_Service & CaseId≠null & 보증판정≠판정대기 → `Is_Billable__c` 상속. **CaseId·RecordType이 INSERT 시점 필수**
  - `WO_WOLI_Generate`(after-create): WorkTypeId 있으면 WOLI+SA+종속성+ProductRequired 자동 생성, Territory 비면 "중부권" 자동. **WorkTypeId 필수, Territory 공란 OK**
  - `WOLI_Skill_Requirement_Generate`(WOLI after-create): 자동
- **VR `Pricebook_Required_For_Paid_Service`**: Field_Service인데 Pricebook2Id 비면 저장 실패 → Case 경로는 Pricebook2Id 필수

### 4개 결정
1. Case Pricebook2Id → **표준 PB(IsStandard=true) 조회해 채우기만**
2. Order Contact → **ShipToContactId**
3. VRA = 레이아웃 **원격지원 섹션 3필드**(`Remote_Support_Requested__c`/`_At__c`/`_By__c`) → **INSERT/UPDATE 안 함**
4. Order StartDate → **Actual_Ship_Date__c + 1, 없으면 오늘 + 7**
5. Address 폴백 → Order경로 `Order.Shipping* → Account.Shipping*`, Case경로 `선택Asset.* → Account.Shipping*` (판정기준: Street 공란 여부)

### 최종 필드 매핑
| 항목 | Order 경로 | Case 경로 |
|---|---|---|
| 진입 | RecordType `Installation_Commissioning`, `Related_Order__c`=recordId | RecordType `Field_Service`, `CaseId`=recordId |
| WorkType | 화면 라디오(신규설치/설비교체)→Get Id | 고정 `고장 진단·조치`→Get Id |
| Account/Contact | Order.AccountId / **ShipToContactId** | Case.AccountId / ContactId |
| Asset | 교체 시 화면 필수, 신규 공란 | Case.AssetId 프리필(편집 가능) |
| StartDate | 출하일+1 / 오늘+7 | 희망방문일(기본 내일) |
| Pricebook2Id | — | 표준 PB |
| Address | Order.Shipping→Account.Shipping | 선택Asset→Account.Shipping (위경도 있으면 복사) |
| 위임 | Territory/WOLI/SA | +Is_Billable__c |
| 제외 | 원격지원(VRA), EndDate, Duration/DurationType | 동일 + Quoted_Amount__c |
| 끝 | Navigate → 생성된 WorkOrder | 동일 |

### 진행 상태 / 막힌 지점
- 스킬 `automation-flow-generate` 로드 완료.
- **3단계 파이프라인 Step 1(`fetchGroundedObjectMetadata`)이 3회 모두 `Unexpected error while invoking tool executeMetadataAction`로 실패** (최소 프롬프트로도 실패 → 도구 일시 장애로 추정).
- 스킬 규칙상 flow XML 수동 작성 금지 → **도구 복구 후 재시도 필요.**

### 재개 시 할 일
1. `execute_metadata_action` Step1~3 재시도(위 userPrompt로), isComplete까지 루프
2. 생성된 flow 배포
3. Order·Case 각 1건으로 실제 실행 → WO/WOLI/SA 생성 검증
4. 두 오브젝트에 Quick Action 배치

---

나중에 돌려볼 때 이 요약 그대로 이어가면 됩니다. 메모리에 저장해둘까요?