- erd 그리기 + 데포르매
---
case to wo, order to wo

주소 매핑까지 반영된 최종 상태 기준으로, 두 플로우(Order→설치 WO, Case→서비스 WO)가 정상 동작하려면 각 객체에 갖춰져야 하는 필드를 정리합니다.

생성 완료 창 뜨는 case to wo, order to wo
case to wo 시 startdate enddate 지정 관련

product required 관련 checkbox 표시 layout에 work order
work order 03 30 삭제 예정

case sa due date 고려 필요 필요하긴 한데 wo에 맞추면 되지 않을까

acct 주소 한글

대한케미컬 asset 관련 모델명 및 세팅

빈 껍데기 에셋

---

# 객체별 필수/권장 필드 (최종)

## Account
| 필드 / 조건 | 등급 | 이유 |
|---|---|---|
| **연결된 Contact 1건 이상** | ✅ Order 경로 사실상 필수 | Order에 ShipToContact 없을 때 Account의 Contact를 WO.ContactId로 사용. 0건이면 담당자 필수 검증에서 실패 |
| **연결된 Asset 1건 이상** | ✅ 필수 | 두 플로우의 Asset 드롭다운이 `Asset.AccountId=이 Account`로 필터. 없으면 선택지 없음 |
| ShippingStreet / ShippingCity / ShippingState / ShippingPostalCode / ShippingCountry | ⚠️ 권장 | WO 주소로 복사됨. 비면 WO 주소가 공란(오류는 아님) |

## Asset
| 필드 | 등급 | 이유 |
|---|---|---|
| **AccountId** | ✅ 필수 | Asset 드롭다운 필터 기준. 없으면 목록에 안 뜸 |
| Name | ✅ (표준) | 드롭다운 표시 라벨 |

## Order (→ 설치 작업오더)
| 필드 | 등급 | 이유 |
|---|---|---|
| **AccountId** | ✅ 필수 | WO.AccountId → 고객사 필수 검증 |
| **Actual_Ship_Date__c** | ✅ 필수 | 없으면 안내화면에서 중단. WO StartDate(+1일 09:00)/EndDate(+9일 18:00) 산출 근거 |
| **ShipToContactId** | ⚠️ 권장 | WO.ContactId 1순위. 비면 Account Contact로 대체 → Account에 Contact도 없으면 실패 |

## Case (→ 서비스 작업오더)
| 필드 | 등급 | 이유 |
|---|---|---|
| **AccountId** | ✅ 필수 | WO.AccountId → 고객사 필수 검증 |
| **ContactId** | ✅ 필수 | WO.ContactId → 담당자 필수 검증. **대체 로직 없음** — 비면 무조건 실패 |
| Priority | ⚠️ 권장 | WO EndDate 산출(긴급1/보통3/낮음7/기타3일). 비어도 fallback 3일로 동작 |
| AssetId | 선택 | 화면에서 다시 선택하므로 없어도 무방 |

---

## 성공 최소 조건 요약

**Order 경로**
```
Order.AccountId + Order.Actual_Ship_Date__c
+ (Order.ShipToContactId 또는 Account에 Contact 1건 이상)
+ Account에 Asset 1건 이상
```

**Case 경로**
```
Case.AccountId + Case.ContactId
+ Account에 Asset 1건 이상
```

## 공통 전제 (레코드 아님 — org 셋업 데이터)
- ServiceTerritory `중부권`
- WorkType: `신규 설치`·`설비 교체`(Order) / `고장 진단·조치`(Case)
- WorkOrder RecordType: `Installation_Commissioning`(Order) / `Field_Service`(Case)
- 표준 가격표 (Case — Field_Service 검증규칙 때문, 플로우가 자동 주입)

> 위 필수(✅) 중 하나라도 빠지면 검증규칙 위반 또는 fault로 WO 생성 실패, 기준 데이터가 없으면 Error_Screen으로 빠집니다. 권장(⚠️) 항목은 없어도 생성은 되지만 값이 비거나 기본값이 적용됩니다.

- **주소(⚠️)**는 이제 Account Shipping → WO로 복사되지만, 두 플로우의 **필수 조건은 아닙니다**. 방문 주소를 WO에 표시하려면 Account에 Shipping 주소를 채워두세요.

1. 생성: SA 미배정(`None`)으로 생성 (지금 이 org 자동화가 하는 부분)
2. Schedule: 최적화/규칙으로 엔지니어·시간대 배정 → `Scheduled`
3. Dispatch: 디스패처가 콘솔에서 쓱 검토 후 다중선택 → 일괄 확정 → `Dispatched` (모바일로 통지)
4. 현장 도착: 엔지니어가 앱에서 `In Progress` 찍음 → 실제 작업 시작
5. 완료: `Completed`




설비 출하 일정은 현장 반입 가능일 및 설치 착수일(Target Date)을 기준으로 역산(Backward Scheduling)하여 잡는 것이 일반적입니다.

설비는 부피가 크고 정밀하며 고가인 경우가 많아, 현장에 도착하자마자 바로 설치 라인으로 직행(Direct In-feed)시키는 것이 보관 비용과 품질 리스크를 줄이는 기본 원칙입니다.

**출하 일정 산정 시 고려하는 주요 요소**

- **운송 및 통관 리드타임:**
    
    - **국내 운송:** 상·하차 및 육상 운송 (보통 1~2일 소요).
        
    - **해외 운송:** 해상 운송(2~4주 이상) 또는 항공 운송(3~5일) 외에 현지 수출입 통관(Customs Clearance) 기간을 필수 반영.
        
- **현장 준비 상태 (Site Readiness):**
    
    - 바닥 기초 공사, 전력·용수·가스·배기 등 1차 유틸리티 인프라 완공 여부.
        
    - 반입 경로(출입문 크기, 엘리베이터/양중구 개방) 및 클린룸 환경 조건 확보 여부.
        
- **양중(도비) 및 인력 스케줄링:**
    
    - 대형 크레인, 특수 운송 지게차 예약 일정.
        
    - 설비 셋업 전문 엔지니어의 현장 투입 타이밍.
        
- **현장 야적/보관 리스크 방지:**
    
    - 현장 공사가 덜 끝난 상태에서 조기 출하되면 외부 창고 보관료가 발생하고, 온·습도 변화나 분진으로 인해 정밀 설비 부품이 손상될 위험이 큽니다.
        

**실무적인 주의점**

현장 공사(건축/인프라)는 지연 변수가 자주 발생합니다. 따라서 통상 출하 2~4주 전 현장 점검(Site Survey)을 통해 반입 가능 여부를 최종 확인한 뒤 출하 승인을 내리며, 공사가 밀렸다면 제작처 창고 보관(Hold)이나 출하 연기를 조율합니다.